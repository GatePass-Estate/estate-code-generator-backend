[tool.poetry]
name = "gatepass-docs"
version = "0.1.0"
description = "Shared document helpers for GatePass services"
authors = ["GatePass"]
packages = [{ include = "gatepass_docs" }]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
